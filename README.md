# Getting Started

Run the following commands to config your system

Install a SSH Key


```
git clone git@github.com:Tbraxton07/classDemo.git
cd classDemo

sudo apt update
sudo apt upgrade -y
sudo apt install build-essential

g++ -o hello main.cpp
./hello
```

# Build Process

Run the following commands to build!

# Contributors:

* Shawn Tunningley
* Michael Prado
* Trenten Braxton
* Chapman